name = "turnstile-verify"
main = "src/index.js"
compatibility_date = "2024-01-01"

# Store your Turnstile secret key as an encrypted secret (never in plain text):
#   wrangler secret put TURNSTILE_SECRET
# Then paste your secret key when prompted.
